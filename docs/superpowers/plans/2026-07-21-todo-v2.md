# Todo v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The five widenings of the todo-v2 spec (#156–#160) plus the upgraded, remeasured app (#161).

**Architecture:** Generator-only throughout (runtime exports `computed`/`list` already). Execution order = risk order: W1 lifecycle → W5 computed → W4 keyboard → W2 JSON → W3 if-in-row → W6 app.

**Tech Stack:** existing repo toolchain only.

## Global Constraints

Same as every program: runtime firewall empty; TDD per slice (fixture + failing test first); existing snapshots byte-identical unless pinned on purpose; DECISIONS #156–#161 (FR), BENCH n°66 (FR, append-only), HARNESS 1.58.0→1.59.0; commits `type(scope): … (DECISIONS #N)`; CI 3-OS green before done.

---

### Task 1 — W1 lifecycle (#156)

**Files:** `src/Filament.Generator/CSharpFrontEnd.cs` (member walk: the `[unsupported-modifier] 'override'` site; admit exactly `OnInitialized`/`OnInitializedAsync`, no params, void/Task; record as InitMethod; refuse `OnParametersSet`/`OnAfterRender`/`Dispose` overrides BY NAME with guidance), `TemplateCompiler.cs` (emit the call after the prologue, BEFORE create); fixtures `Supported/Code/OnInit.razor` (sync: `msg = "ready"` → first paint shows "ready"), `Supported/Code/OnInitAsync.razor` (async: sync prefix + `await Task.Delay` + write); `Unsupported/Code/OnAfterRender.razor`; tests in new `TodoV2Tests.cs` — pins: `onInitialized();` (or inlined body) appears BEFORE `document.createElement` in the module; async form called un-awaited; boundary refusal message.

- [ ] fixtures + failing tests → implement → full suite green → commit `feat(lifecycle): OnInitialized(Async) admitted -- the body runs once, before the first paint (DECISIONS #156)`

### Task 2 — W5 computed (#160)

**Files:** `CSharpFrontEnd.cs` (member walk: PropertyDeclaration with ExpressionBody, private, get-only, subset type → PropInfo-like member emitting `const {js} = computed(() => {expr});` in prologue after fields; reads resolve `IPropertySymbol` declared in the component → `{js}.value`; SlotIsReactive true iff body reads reactive state); `TemplateCompiler.cs` import via `_primitives.Add("computed")`. Fixtures `Supported/Code/ComputedProp.razor` (the probe file: `Active => xs.Where(x=>x>1).Count()` displayed + xs reassigned) ; `Unsupported/Code/ComputedSetter.razor` (property with setter still refused). Pins: `const active = computed(() => xs.value.filter(x => x > 1).length);`, template effect reads `active.value`, import line contains `computed`.

- [ ] fixtures + failing tests → implement → suite → commit `feat(state): expression-bodied properties become computed() -- the runtime's unused export, used (DECISIONS #160)`

### Task 3 — W4 keyboard (#159)

**Files:** `CSharpFrontEnd.cs` (Method(): admit ONE parameter typed `Microsoft.AspNetCore.Components.Web.KeyboardEventArgs`; MemberAccess arm on that parameter symbol: allowlist Key→key, Code→code, CtrlKey/ShiftKey/AltKey/MetaKey→ctrlKey/…, else refuse naming the list; TypeSubset: KeyboardEventArgs admitted as PARAMETER type only — fields/returns of it still refuse), `TemplateCompiler.cs` (the handler path already passes the DOM event: verify `listen(el,'keydown', onKey)` and that NamedByTemplate/mustBeCallable admits the 1-param method for onkeydown/onkeyup ONLY — a click handler naming it still refuses). Fixtures `Supported/Code/KeyDown.razor` (the probe file), `Unsupported/Code/KeyDownLocation.razor` (`e.Location` → refusal), `Unsupported/Code/KeyOnClick.razor` (1-param handler on @onclick refused). Pins: `function onKey(e)`, `e.key === 'Enter'`, listen wiring.

- [ ] fixtures + failing tests → implement → suite → commit `feat(events): KeyboardEventArgs on keydown/keyup -- e.Key is the DOM event's own key (DECISIONS #159)`

### Task 4 — W2 local JSON (#157)

**Files:** `CSharpFrontEnd.cs` (dispatcher: containing type `System.Text.Json.JsonSerializer`; `Serialize(x)` → `JSON.stringify(__serT(x))`, `Deserialize<T>(s)` → `__desT(JSON.parse(s))`; options argument → refusal; T through `TypeSubset.JsonUnfaithful`), `TemplateCompiler.cs` (per-record helper emission from RecordInfo: `__ser{R}` writes DECLARED names, reads `.value` off signal props; `__des{R}` reads declared names case-sensitively, wraps mutated props in `signal(…)`; List → `.map`; scalars identity → no helper). Fixture `Supported/Code/JsonRoundTrip.razor` (record with a mutated bool prop — the signal case — serialize into a field, deserialize back, display); `Unsupported/Code/JsonOptions.razor`, `Unsupported/Code/JsonLong.razor`. Pins: `JSON.stringify(__serItem(`, `__desItem` wraps `done: signal(`, declared-name keys (`Id:`/`'Id'`), long refusal reuses the #147 message shape.

- [ ] fixtures + failing tests → implement → suite → commit `feat(json): JsonSerializer.Serialize/Deserialize under the #147 shape gate -- record-shape-aware, signal props unwrapped/rewrapped (DECISIONS #157)`

### Task 5 — W3 @if in a row (#158)

**Files:** `TemplateCompiler.cs` (reassembly: emit a nested region's method as a LOCAL FUNCTION inside its parent region's method so the loop variable captures — the decision-141 idiom; RegionOps/EmitOps: inside a row create, a nested If op emits an anchored `list()` whose source reads the translated condition — reuse the existing @if lowering, row-scoped), `CSharpFrontEnd.cs` if the ops table needs a row-context arm. Fixture: the probe `IfInRow.razor` shape (badge under `@if (t.Done)`) → `Supported/Code/IfInRow.razor`; boundary `Unsupported/Code/ForeachInRow.razor` (nested @foreach still refused, located). Pins: createT contains `list(` with source `t.done.value ? [0] : []` (or the ranges equivalent); the badge markup lives in the nested create; boundary message.
Fallback (decide in-task, disclose in DECISIONS): if the reassembly nesting destabilizes the marker/ordering machinery beyond a day's honest work, refuse with a message naming the `hidden="@(…)"` idiom and adapt W6's edit flow to it.

- [ ] fixtures + failing tests → implement (or disclosed fallback) → suite → commit `feat(control-flow): @if inside a @foreach row -- the nested region compiles in the loop's scope, renders as a row-anchored list() (DECISIONS #158)`

### Task 6 — W6 the app, measured (#161, BENCH n°66)

**Files:** `baseline/Todo.Blazor/App.razor` (+`_Imports`/csproj if needed for IJSRuntime+System.Text.Json), `samples/Todo/todo.js` (answer key updated), `Snapshots/Todo.approved.js` (re-pinned deliberately), `bench/harness/bench.mjs` (todo driver v2 + HARNESS 1.59.0: clear storage → add via click + add via ENTER → toggle → assert DOM + `localStorage.getItem('todos')` BYTE-equal → in-place edit (start/change/commit) → `ctx.page.reload()` → assert restored → remove/clear paths), `examples/TodoTailwind/*` (same sources + README), docs (README counts, REAL-APPS lifecycle/JSON/keyboard notes), DECISIONS #161 + BENCH n°66, memory.

- [ ] baseline `dotnet build` FIRST → generator compiles → answer key + pins updated → blazor publish + filament build → contract green on BOTH shells (byte-equal stored JSON is the W2 oracle) → docs → commit `feat(todo): todo v2 -- persistence, Enter, in-place edit, computed count, remeasured (DECISIONS #161, BENCH n°66)` → push → CI watch.

## Self-review

Spec coverage: W1→T1, W5→T2, W4→T3, W2→T4, W3→T5, W6→T6 ✓. Probe files referenced exist in scratchpad and are re-authored as fixtures ✓. Names used consistently (`onInitialized`, `__serItem/__desItem`, `active`, `onKey`) ✓. The one open decision (T5 fallback) is scoped and disclosed ✓.
