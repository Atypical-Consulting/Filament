@inherits ChainGrand

@* THE MIDDLE LINK. It adds nothing of its own -- it exists to prove the merge follows the chain PAST
   the first link. Its markup is irrelevant (the derived overrides BuildRenderTree); it only forwards
   the @inherits so ChainGrand's members reach ChainApp. *@
