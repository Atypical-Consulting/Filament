<p><span id="counter-value">@currentCount</span></p>

@code {
    private int currentCount = 0;

    private void Increment()
    {
        switch (currentCount) { case 0: currentCount++; break; }
    }
}
