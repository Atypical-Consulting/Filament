@* The COMPOSITION half of decision 151: the multi-token Tailwind value a parent binds to a
   child's string parameter must also keep its spaces -- EmitComposition folds the attribute's
   value nodes with the same prefix-aware concat the static path uses. *@

<div id="wrap"><TwCard Label="alpha beta" Cls="w-1/2 hover:bg-amber-400 focus:ring-2 max-w-[42rem] sm:px-4 -mt-2 md:grid-cols-[1fr_2fr]" /></div>
