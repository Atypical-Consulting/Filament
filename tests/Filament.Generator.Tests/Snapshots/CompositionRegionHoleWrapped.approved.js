// GENERATED by Filament.Generator from RegionHoleWrapped.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, setText, listen, insert, list } from '../../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const count = signal(0);

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('div');
  _el0.id = 'wrap';
  const _el1 = document.createElement('div');
  _el1.id = 'card';
  const _if0 = document.createComment('');
  insert(_el1, _if0);
  insert(_el0, _el1);
  const _el4 = document.createElement('button');
  _el4.id = 'inc';
  insert(_el4, document.createTextNode('inc'));
  insert(_el0, _el4);

  // -- bindings ---------------------------------------------------------------
  function ifBody() {
    const _el2 = document.createElement('div');
    _el2.id = 'hole';
    const _el3 = document.createElement('span');
    _el3.id = 'body';
    const _tx0 = document.createTextNode('');
    insert(_el3, _tx0);
    insert(_el2, _el3);
    effect(() => setText(_tx0, count.value));
    return _el2;
  }
  list(_el1, () => ('t' === 't') ? [0] : [], () => 0, ifBody, _if0);

  // -- events -----------------------------------------------------------------
  listen(_el4, 'click', () => {
    count.value++;
  });

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
}
