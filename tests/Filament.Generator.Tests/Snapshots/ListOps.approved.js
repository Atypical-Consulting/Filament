// GENERATED by Filament.Generator from App.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, batch, setAttr, listen, insert, list } from '../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const items = [];
  const itemsVersion = signal(0);

  function itemsChanged() {
    itemsVersion.value++;
  }

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('ul');
  _el0.id = 'list';
  const _el1 = document.createElement('button');
  _el1.id = 'add';
  insert(_el1, document.createTextNode('add'));
  insert(_el0, _el1);
  const _el2 = document.createElement('button');
  _el2.id = 'clear';
  insert(_el2, document.createTextNode('clear'));
  insert(_el0, _el2);

  // -- bindings ---------------------------------------------------------------
  function createItem(item) {
    const _el3 = document.createElement('li');
    setAttr(_el3, 'class', 'row');
    insert(_el3, document.createTextNode(item));
    return _el3;
  }
  list(_el0, () => {
    itemsVersion.value;
    return items;
  }, (item) => item, createItem, null);

  // -- events -----------------------------------------------------------------
  listen(_el1, 'click', () => batch(() => {
    items.push('alpha');
    items.push('beta');
    items.push('gamma');
    itemsChanged();
  }));
  listen(_el2, 'click', () => {
    items.length = 0;
    itemsChanged();
  });

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
}
