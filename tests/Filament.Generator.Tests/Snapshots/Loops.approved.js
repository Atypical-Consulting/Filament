// GENERATED by Filament.Generator from App.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, batch, setText, listen, insert } from '../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const n = signal(0);

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('h1');
  _el0.id = 'title';
  insert(_el0, document.createTextNode('Loops'));
  const _el1 = document.createElement('p');
  insert(_el1, document.createTextNode('n = '));
  const _el2 = document.createElement('span');
  _el2.id = 'v';
  const _tx0 = document.createTextNode('');
  insert(_el2, _tx0);
  insert(_el1, _el2);
  const _el3 = document.createElement('button');
  _el3.id = 'bwhile';
  insert(_el3, document.createTextNode('while'));
  const _el4 = document.createElement('button');
  _el4.id = 'bswitch';
  insert(_el4, document.createTextNode('switch'));
  const _el5 = document.createElement('button');
  _el5.id = 'bdo';
  insert(_el5, document.createTextNode('do'));

  // -- bindings ---------------------------------------------------------------
  effect(() => setText(_tx0, n.value));

  // -- events -----------------------------------------------------------------
  listen(_el3, 'click', () => batch(() => {
    n.value = 0;
    while (n.value < 5) {
      n.value = n.value + 1;
    }
  }));
  listen(_el4, 'click', () => batch(() => {
    switch (n.value) {
    case 5:
      n.value = 9;
      break;
    default:
      n.value = 0;
      break;
    }
  }));
  listen(_el5, 'click', () => batch(() => {
    n.value = 0;
    do {
      n.value = n.value + 1;
    } while (n.value < 3);
  }));

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el1);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el3);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el4);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el5);
}
