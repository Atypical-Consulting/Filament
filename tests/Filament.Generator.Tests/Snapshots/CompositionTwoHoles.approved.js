// GENERATED by Filament.Generator from TwoHoles.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, setText, listen, insert } from '../../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const count = signal(0);

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('div');
  _el0.id = 'wrap';
  const _el1 = document.createElement('div');
  _el1.id = 'card';
  const _el2 = document.createElement('div');
  _el2.id = 'head';
  insert(_el1, _el2);
  const _el3 = document.createElement('div');
  _el3.id = 'body';
  const _el4 = document.createElement('span');
  _el4.id = 'mark';
  const _tx0 = document.createTextNode('');
  insert(_el4, _tx0);
  insert(_el3, _el4);
  insert(_el1, _el3);
  insert(_el0, _el1);
  const _el5 = document.createElement('button');
  _el5.id = 'inc';
  insert(_el5, document.createTextNode('inc'));
  insert(_el0, _el5);

  // -- bindings ---------------------------------------------------------------
  effect(() => setText(_tx0, count.value));

  // -- events -----------------------------------------------------------------
  listen(_el5, 'click', () => {
    count.value++;
  });

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
}
