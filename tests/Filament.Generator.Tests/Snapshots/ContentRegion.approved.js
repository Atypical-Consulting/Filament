// GENERATED by Filament.Generator from App.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, batch, setText, setAttr, listen, insert, list } from '../../src/filament-runtime/src/index.ts';

// -- @code: immutable literal data, hoisted to module scope ------------------
const _pool = [3, 4, 1, 5, 2];

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const level = 1;
  const count = signal(0);
  const show = signal(true);
  const saved = signal('');
  const model = { name: signal('') };
  const items = signal([1, 2, 3]);

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('div');
  _el0.id = 'head';
  insert(_el0, document.createTextNode('head'));
  const _el1 = document.createElement('ul');
  _el1.id = 'list';
  const _el3 = document.createElement('p');
  _el3.id = 'tail';
  insert(_el3, document.createTextNode('tail'));
  const _el4 = document.createElement('div');
  _el4.id = 'card';
  const _if0 = document.createComment('');
  insert(_el4, _if0);
  const _el6 = document.createElement('form');
  const _if1 = document.createComment('');
  insert(_el6, _if1);
  const _el9 = document.createElement('p');
  _el9.id = 'out';
  const _tx1 = document.createTextNode('');
  insert(_el9, _tx1);
  const _el10 = document.createElement('button');
  _el10.id = 'toggle';
  insert(_el10, document.createTextNode('toggle'));
  const _el11 = document.createElement('button');
  _el11.id = 'add';
  insert(_el11, document.createTextNode('add'));

  // -- bindings ---------------------------------------------------------------
  function createN(n) {
    const _el2 = document.createElement('li');
    insert(_el2, document.createTextNode(n));
    return _el2;
  }
  list(_el1, () => items.value, (n) => n, createN, null);
  function ifBody() {
    const _el5 = document.createElement('span');
    _el5.id = 'body';
    const _tx0 = document.createTextNode('');
    insert(_el5, _tx0);
    effect(() => setText(_tx0, count.value));
    return _el5;
  }
  list(_el4, () => (show.value) ? [0] : [], () => 0, ifBody, _if0);
  function ifBody1_0() {
    const _el7 = document.createElement('input');
    _el7.id = 'name';
    effect(() => { _el7.value = model.name.value; });
    listen(_el7, 'change', (e) => { model.name.value = e.target.value; });
    return _el7;
  }
  function ifBody1_1() {
    const _el8 = document.createElement('button');
    _el8.id = 'save';
    setAttr(_el8, 'type', 'submit');
    insert(_el8, document.createTextNode('save'));
    return _el8;
  }
  list(_el6, () => (show.value) ? [0, 1] : [], (i) => i, (i) => i === 0 ? ifBody1_0() : ifBody1_1(), _if1);
  effect(() => setText(_tx1, saved.value));

  // -- events -----------------------------------------------------------------
  listen(_el6, 'submit', (e) => {
    e.preventDefault();
    saved.value = model.name.value;
  });
  listen(_el10, 'click', () => {
    show.value = !show.value;
  });
  listen(_el11, 'click', () => batch(() => {
    count.value++;
    items.value = _pool.filter(x => x !== 2);
  }));

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
  insert(target, document.createTextNode('\n'));
  insert(target, _el1);
  insert(target, document.createTextNode('\n'));
  insert(target, _el3);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el4);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el6);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el9);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el10);
  insert(target, document.createTextNode('\n'));
  insert(target, _el11);
}
