// GENERATED by Filament.Generator from Counter.razor. DO NOT EDIT.
//
// The template is compiled; the @code block is hand-written JS, spliced verbatim
// (Phase 2: "la logique @code reste ecrite en JS a la main").

import { signal, effect, setText, listen, insert } from '../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code ------------------------------------------------------------------
  const currentCount = signal(0);

  const Increment = () => {
      currentCount.value++;
  };

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('h1');
  _el0.id = 'title';
  insert(_el0, document.createTextNode('Counter'));
  const _el1 = document.createElement('p');
  insert(_el1, document.createTextNode('Current count: '));
  const _el2 = document.createElement('span');
  _el2.id = 'counter-value';
  const _tx0 = document.createTextNode('');
  insert(_el2, _tx0);
  insert(_el1, _el2);
  const _el3 = document.createElement('button');
  _el3.id = 'increment';
  insert(_el3, document.createTextNode('Click me'));

  // -- bindings ---------------------------------------------------------------
  effect(() => setText(_tx0, currentCount.value));

  // -- events -----------------------------------------------------------------
  listen(_el3, 'click', Increment);

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el1);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el3);
}
