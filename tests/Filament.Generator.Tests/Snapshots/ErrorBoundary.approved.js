// GENERATED by Filament.Generator from ErrorBoundary.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, insert, list } from '../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const fail = true;

  function risky() {
    if (fail) {
      throw new Error('the content could not be rendered');
    }
    return 'ok';
  }

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('div');
  _el0.id = 'wrap';
  const _el1 = document.createElement('p');
  _el1.id = 'outside';
  insert(_el1, document.createTextNode('outside'));
  insert(_el0, _el1);
  const _ebErr0 = signal(null);
  const _ebAnchor0 = document.createComment('');
  insert(_el0, _ebAnchor0);

  // -- bindings ---------------------------------------------------------------
  function _ebBody0_0() {
    try {
      const _el2 = document.createElement('p');
      _el2.id = 'in';
      insert(_el2, document.createTextNode(risky()));
      return _el2;
    } catch (_e) {
      _ebErr0.value ??= _e;
      return document.createTextNode('');
    }
  }
  function _ebError0_1() {
    const _el3 = document.createElement('p');
    _el3.id = 'err';
    insert(_el3, document.createTextNode('Sorry: '));
    insert(_el3, document.createTextNode(_ebErr0.value.message));
    return _el3;
  }
  list(_el0, () => _ebErr0.value === null ? [0] : [1], (i) => i, (i) => i === 0 ? _ebBody0_0() : _ebError0_1(), _ebAnchor0);

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
}
