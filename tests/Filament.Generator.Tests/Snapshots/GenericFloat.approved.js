// GENERATED by Filament.Generator from GenericFloat.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, setText, listen, insert } from '../../../src/filament-runtime/src/index.ts';

// -- float display: shortest decimal that round-trips through float32 (C# float.ToString) --
function __f32(x) {
  x = Math.fround(x);
  if (!isFinite(x) || Number.isInteger(x)) return String(x);
  for (let p = 1; p <= 9; p++) {
    const s = x.toPrecision(p);
    if (Math.fround(Number(s)) === x) return Number(s).toString();
  }
  return String(x);
}

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const ratio = signal(Math.fround(0.1));

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('div');
  _el0.id = 'wrap';
  const _el1 = document.createElement('span');
  _el1.id = 'out';
  const _tx0 = document.createTextNode('');
  insert(_el1, _tx0);
  insert(_el0, _el1);
  const _el2 = document.createElement('button');
  _el2.id = 'bump';
  insert(_el2, document.createTextNode('bump'));
  insert(_el0, _el2);

  // -- bindings ---------------------------------------------------------------
  effect(() => setText(_tx0, __f32(ratio.value)));

  // -- events -----------------------------------------------------------------
  listen(_el2, 'click', () => {
    ratio.value += Math.fround(0.1);
  });

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
}
