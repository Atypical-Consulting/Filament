// GENERATED by Filament.Generator from App.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, setText, setAttr, listen, insert } from '../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const model = { name: signal('') };
  const saved = signal('');

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('form');
  const _el1 = document.createElement('input');
  _el1.id = 'name';
  insert(_el0, _el1);
  const _el2 = document.createElement('button');
  _el2.id = 'save';
  setAttr(_el2, 'type', 'submit');
  insert(_el2, document.createTextNode('save'));
  insert(_el0, _el2);
  const _el3 = document.createElement('p');
  _el3.id = 'live';
  const _tx0 = document.createTextNode('');
  insert(_el3, _tx0);
  const _el4 = document.createElement('p');
  _el4.id = 'out';
  const _tx1 = document.createTextNode('');
  insert(_el4, _tx1);

  // -- bindings ---------------------------------------------------------------
  effect(() => { _el1.value = model.name.value; });
  effect(() => setText(_tx0, model.name.value));
  effect(() => setText(_tx1, saved.value));

  // -- events -----------------------------------------------------------------
  listen(_el1, 'change', (e) => { model.name.value = e.target.value; });
  listen(_el0, 'submit', (e) => {
    e.preventDefault();
    saved.value = model.name.value;
  });

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
  insert(target, _el3);
  insert(target, _el4);
}
