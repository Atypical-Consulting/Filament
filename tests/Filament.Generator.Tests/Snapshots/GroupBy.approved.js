// GENERATED by Filament.Generator from App.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, effect, batch, setText, listen, insert } from '../../src/filament-runtime/src/index.ts';

// -- @code: immutable literal data, hoisted to module scope ------------------
const _nums = [3, 7, 2, 9, 5];

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const groups = signal(0);
  const firstKey = signal(-1);
  const firstSize = signal(0);

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('p');
  insert(_el0, document.createTextNode('G:'));
  const _el1 = document.createElement('span');
  _el1.id = 'g';
  const _tx0 = document.createTextNode('');
  insert(_el1, _tx0);
  insert(_el0, _el1);
  insert(_el0, document.createTextNode(' K:'));
  const _el2 = document.createElement('span');
  _el2.id = 'k';
  const _tx1 = document.createTextNode('');
  insert(_el2, _tx1);
  insert(_el0, _el2);
  insert(_el0, document.createTextNode(' S:'));
  const _el3 = document.createElement('span');
  _el3.id = 's';
  const _tx2 = document.createTextNode('');
  insert(_el3, _tx2);
  insert(_el0, _el3);
  const _el4 = document.createElement('button');
  _el4.id = 'go';
  insert(_el4, document.createTextNode('go'));

  // -- bindings ---------------------------------------------------------------
  effect(() => setText(_tx0, groups.value));
  effect(() => setText(_tx1, firstKey.value));
  effect(() => setText(_tx2, firstSize.value));

  // -- events -----------------------------------------------------------------
  listen(_el4, 'click', () => batch(() => {
    groups.value = [..._nums.reduce((__m, __x) => { const __k = (x => x % 2)(__x); let __g = __m.get(__k); if (!__g) { __g = []; __g.key = __k; __m.set(__k, __g); } __g.push(__x); return __m; }, new Map()).values()].length;
    firstKey.value = [..._nums.reduce((__m, __x) => { const __k = (x => x % 2)(__x); let __g = __m.get(__k); if (!__g) { __g = []; __g.key = __k; __m.set(__k, __g); } __g.push(__x); return __m; }, new Map()).values()][0].key;
    firstSize.value = [..._nums.reduce((__m, __x) => { const __k = (x => x % 2)(__x); let __g = __m.get(__k); if (!__g) { __g = []; __g.key = __k; __m.set(__k, __g); } __g.push(__x); return __m; }, new Map()).values()][0].length;
  }));

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
  insert(target, document.createTextNode('\n\n'));
  insert(target, _el4);
}
