// GENERATED by Filament.Generator from If.razor. DO NOT EDIT.
//
// Compiled from pure .razor: the template AND the @code block (Phase 3).
// State is lifted -- a private field the template reads and something assigns
// becomes a Signal -- and no user text is spliced anywhere in this file.

import { signal, listen, insert, list } from '../../src/filament-runtime/src/index.ts';

export function mount(target) {
  // -- @code: state and behaviour, compiled from C# ---------------------------
  const show = signal(true);

  // -- create(): the tree, built detached -------------------------------------
  const _el0 = document.createElement('div');
  _el0.id = 'wrap';
  const _el1 = document.createElement('button');
  _el1.id = 't';
  insert(_el1, document.createTextNode('t'));
  insert(_el0, _el1);
  const _if0 = document.createComment('');
  insert(_el0, _if0);

  // -- bindings ---------------------------------------------------------------
  function ifBody() {
    const _el2 = document.createElement('span');
    _el2.id = 'msg';
    insert(_el2, document.createTextNode('hi'));
    return _el2;
  }
  list(_el0, () => (show.value) ? [0] : [], () => 0, ifBody, _if0);

  // -- events -----------------------------------------------------------------
  listen(_el1, 'click', () => {
    show.value = !show.value;
  });

  // -- attach: last, so the effects' first run made no MutationRecord ----------
  insert(target, _el0);
}
