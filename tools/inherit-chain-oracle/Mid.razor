@inherits Grand

@* THE MIDDLE LINK. It adds nothing of its own; it exists to prove the merge follows the chain PAST the
   first link. Its markup is irrelevant (the derived overrides BuildRenderTree); it only forwards the
   @inherits so Grand's members reach App. *@
