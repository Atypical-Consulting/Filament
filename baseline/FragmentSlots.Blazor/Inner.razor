@* The end of the forward: the level that actually places the grandparent's markup. *@
<div id="inner">@ChildContent</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
